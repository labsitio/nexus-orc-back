// Placeholder de fundação (T001) — comprova que tsc --strict roda no monorepo vazio.
// A estrutura real de bounded contexts é responsabilidade de T004 (#9).
export const NEXO_VERSION = "0.1.0" as const;
