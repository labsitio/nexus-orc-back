# QA — T012 (issue #17) — PR #397

SPEC_ID: 001-ingestao-classificacao-orcamentos
PR: labsitio/nexus-orc-back#397
Branch: feat/iac-s3-bucket-orcamentos-raw
Commit testado: 1b2204a
Tipo: primeira validação, IaC pura (sem deploy em conta AWS)

## Escopo

Provisionamento (não aplicado) de bucket S3 `nexo-orcamentos-raw` via AWS CDK v2 —
versionamento, SSE-KMS, imutabilidade via S3 Object Lock (GOVERNANCE, 1825 dias).

## Comandos executados

```
PATH="/home/victor1090/.nvm/versions/node/v24.14.1/bin:$PATH" pnpm install --frozen-lockfile
PATH="...:$PATH" pnpm run lint
PATH="...:$PATH" pnpm run typecheck
PATH="...:$PATH" pnpm run typecheck:infra
PATH="...:$PATH" pnpm exec cdk synth --quiet
PATH="...:$PATH" pnpm run test
```

## Resultados

| Comando | Resultado |
|---|---|
| `pnpm install --frozen-lockfile` | OK, sem erro, lockfile consistente |
| `pnpm run lint` (eslint .) | OK, sem violação |
| `pnpm run typecheck` (tsc --noEmit) | OK |
| `pnpm run typecheck:infra` | OK |
| `pnpm exec cdk synth --quiet` | OK, exit 0, gera `cdk.out/IngestaoIdentificacaoStorageStack.template.json` |
| `pnpm run test` (vitest) | OK — nenhum arquivo de teste ainda (esperado, T004+ não implementadas nesta PR) |

CI (`.github/workflows/ci.yml`) já inclui `typecheck:infra` e `cdk synth --quiet` como
steps antes do `pnpm run test`, na mesma ordem validada aqui.

## Inspeção do template sintetizado

Recurso `AWS::S3::Bucket` (`OrcamentosRawBucketF9B6A8B3`):

```json
{
  "BucketName": "nexo-orcamentos-raw",
  "VersioningConfiguration": { "Status": "Enabled" },
  "ObjectLockEnabled": true,
  "ObjectLockConfiguration": {
    "ObjectLockEnabled": "Enabled",
    "Rule": { "DefaultRetention": { "Days": 1825, "Mode": "GOVERNANCE" } }
  },
  "BucketEncryption": {
    "ServerSideEncryptionConfiguration": [{
      "BucketKeyEnabled": true,
      "ServerSideEncryptionByDefault": {
        "SSEAlgorithm": "aws:kms",
        "KMSMasterKeyID": { "Fn::GetAtt": ["OrcamentosRawKey0B34C4BA", "Arn"] }
      }
    }]
  },
  "PublicAccessBlockConfiguration": {
    "BlockPublicAcls": true, "BlockPublicPolicy": true,
    "IgnorePublicAcls": true, "RestrictPublicBuckets": true
  }
}
```

- `DeletionPolicy: Retain` e `UpdateReplacePolicy: Retain` presentes no bucket
  (`OrcamentosRawBucketF9B6A8B3`) e na chave KMS (`OrcamentosRawKey0B34C4BA`).
- Bucket policy (`OrcamentosRawBucketPolicy5E95A1CF`) contém statement `Deny`
  com `Principal: "*"` e `Condition.Bool."aws:SecureTransport": "false"`,
  cobrindo bucket e `bucket/*`.
- `AWS::KMS::Key` (`OrcamentosRawKey0B34C4BA`) tem `EnableKeyRotation: true`.
  Key policy usa apenas pseudo-parâmetros (`AWS::Partition`, `AWS::AccountId`)
  — sem ARN de conta real hardcoded.
- 1825 dias = 5 * 365, confere com a decisão registrada em comentário no
  código-fonte (`ingestao-identificacao-storage-stack.ts`).
- 1225/1825 dias de retenção: nota explícita no código (`ponytail:` comment)
  registrando que é uma decisão default restritiva pendente de confirmação de
  compliance — condiz com o texto da PR/issue.
- Nenhum `AWS::IAM::Role` no template — correto, fora de escopo de T012 (roles
  de aplicação Lambda são T026/T035/T048/T054).
- Nenhuma credencial, chave de acesso ou ARN de conta real (`arn:aws:iam::<12
  dígitos>`) encontrada no diff nem no template sintetizado
  (`grep -riE "AKIA|aws_secret|BEGIN (RSA|PRIVATE)|arn:aws:iam::[0-9]{12}"` —
  zero ocorrências).

## Diff review

Escopo do diff bate com o esperado: `cdk.json`, `infra/bin/app.ts`,
`infra/lib/ingestao-identificacao-storage-stack.ts`, `infra/tsconfig.json`,
`.github/workflows/ci.yml`, `package.json`, `pnpm-lock.yaml`, `.gitignore`
(adiciona `cdk.out/`), `specs/.../tasks.md` (marca T012 como concluída).
Nenhum arquivo fora de escopo alterado.

`cdk.out/` corretamente listado no `.gitignore` — artefato de synth não deve
ser versionado.

## Riscos residuais / decisões pendentes (não são bugs)

1. **Retenção de 5 anos (GOVERNANCE, 1825 dias)** é um default técnico
   escolhido pelo autor da PR, explicitamente marcado no código como
   "pendente de confirmação de compliance". Precisa de sign-off humano
   (Ricardo/DevOps/compliance) antes de qualquer deploy em produção.
2. **Escolha de AWS CDK v2 (TypeScript)** como ferramenta de IaC em vez de
   Terraform — decisão técnica registrada na PR, também pendente de
   confirmação humana conforme nota em `tasks.md` (T012: "IaC — CDK/Terraform
   a definir por Ricardo/DevOps").
3. Modo GOVERNANCE (não COMPLIANCE) depende de nenhuma IAM policy futura
   conceder `s3:BypassGovernanceRetention` — mitigação documentada no código,
   sem enforcement automatizado nesta PR (não há roles neste diff para
   verificar).

Nenhum dos itens acima é defeito de produto — são decisões de negócio/arquitetura
já sinalizadas no próprio PR, exigindo apenas confirmação humana antes de deploy
real em conta AWS. Esta validação é sobre o template sintetizado; não houve
deploy em nenhuma conta AWS.

## Cobertura

N/A para IaC nesta task — não há lógica de aplicação testável por unit/integration
test tradicional. A "cobertura" aqui é a verificação estrutural do template
sintetizado, documentada acima, mais o smoke test `cdk synth` que já roda no CI.

## Parecer

APROVADO COM RESSALVAS — ver riscos residuais (retenção 5 anos e escolha CDK),
ambos já sinalizados no PR como pendentes de confirmação humana, não bugs.
